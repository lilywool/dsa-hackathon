import type { ReactNode } from "react";

export const metadata = {
  title: "Organization",
};

export default function OrganizationLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
