import type { ReactNode } from "react";

export const metadata = {
  title: "Participant",
};

export default function ParticipantLayout({
  children,
}: {
  children: ReactNode;
}) {
  return children;
}
